import { ComboBox, Button, VerticalBox, HorizontalBox, TextEdit } from "std-widgets.slint";

export component AppWindow inherits Window {
    title: "Sonar - DHCP 局域网诊断工具";
    preferred-width: 1000px;
    preferred-height: 640px;
    background: #f5f5f5;

    in-out property <[string]> network-interfaces;
    in-out property <string> current-mac-address;
    in-out property <string> log-text: "";
    in-out property <string> server-display-text: "（暂无）";
    in-out property <bool> is-scanning: false;

    // 智能滚动：用户滚离底部时暂停跟随，滚回底部时恢复
    in-out property <bool> follow-log: true;

    callback select-interface(string);
    callback start-full-scan();
    callback start-loop-test();
    callback clear-logs();

    // 由 Rust 调用，将日志区滚动到底部
    callback scroll-log-to-bottom();
    scroll-log-to-bottom() => {
        log-edit.viewport-y = min(0px, log-edit.visible-height - log-edit.viewport-height);
    }

    VerticalBox {
        padding: 16px;
        spacing: 10px;

        Text { text: "网卡选择"; font-size: 13px; color: #333333; font-weight: 600; }

        ComboBox {
            model: root.network-interfaces;
            current-index: 0;
            enabled: !root.is-scanning;
            selected(value) => { root.select-interface(value); }
        }

        HorizontalBox {
            spacing: 8px;
            Text { text: "MAC 地址:"; font-size: 12px; color: #666666; }
            Text { text: root.current-mac-address; font-size: 12px; color: #1a73e8; font-weight: 600; }
        }

        Rectangle { height: 1px; background: #e0e0e0; }

        Text { text: "操作"; font-size: 13px; color: #333333; font-weight: 600; }

        HorizontalBox {
            spacing: 10px;
            Button { text: "全面 DHCP 发现"; enabled: !root.is-scanning; clicked => { root.start-full-scan(); } }
            Button { text: "网络环路检测"; enabled: !root.is-scanning; clicked => { root.start-loop-test(); } }
            Button { text: "清空日志"; clicked => { root.clear-logs(); } }
        }

        Rectangle { height: 1px; background: #e0e0e0; }

        // ===== 左右双栏布局 =====
        HorizontalBox {
            spacing: 10px;

            // ===== 左栏：诊断日志（可选中复制） =====
            VerticalBox {
                width: 60%;
                min-width: 400px;

                Text {
                    text: "诊断日志（可直接选中文本复制）";
                    font-size: 13px; font-weight: 600; color: #333333;
                }

                Rectangle {
                    vertical-stretch: 1;
                    border-radius: 4px;
                    border-width: 1px;
                    border-color: #d0d0d0;
                    background: #ffffff;

                    log-edit := TextEdit {
                        text: root.log-text;
                        read-only: true;
                        wrap: word-wrap;
                        font-size: 12px;
                        font-family: "Cascadia Code";
                    }
                }
            }

            // ===== 右栏：DHCP 服务器列表（可选中复制） =====
            VerticalBox {
                width: 40%;
                min-width: 300px;

                Text {
                    text: "发现的服务器（可直接选中文本复制）";
                    font-size: 13px; font-weight: 600; color: #333333;
                }

                Rectangle {
                    vertical-stretch: 1;
                    border-radius: 4px;
                    border-width: 1px;
                    border-color: #d0d0d0;
                    background: #ffffff;

                    srv-edit := TextEdit {
                        text: root.server-display-text;
                        read-only: true;
                        wrap: word-wrap;
                        font-size: 12px;
                        font-family: "Cascadia Code";
                        // 右栏内容少，始终滚到底部
                        viewport-y: min(0px, self.visible-height - self.viewport-height);
                    }
                }
            }
        }
    }
}
